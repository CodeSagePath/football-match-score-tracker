{
  "name": "match-tracker-server",
  "version": "0.1.0",
  "description": "BACKEND SETUP (NODE.JS + EXPRESS + MONGODB) -- Football Match Score Tracker",
  "license": "MIT",
  "author": "CodeSagePath",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
